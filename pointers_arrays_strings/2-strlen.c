#include "main.h"

/**
* strlen - returns the length of a string
* @s - points to the string
* Return: value
*/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i ++)
	{

	}
	return (i);
}
