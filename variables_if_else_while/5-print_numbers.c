#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    int i;

    for (i = 0; i <= 9; i++);
    {
        printf(i + '0'\n); // Convert integer to character
    }
    putchar(''\n);

    return (0);
}